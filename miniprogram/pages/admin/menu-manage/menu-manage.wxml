<!-- pages/admin/menu-manage/menu-manage.wxml -->
<view class="container menu-manage-container">
  <!-- 顶部操作栏 -->
  <view class="header-bar">
    <view class="header-title">
      <text class="title-icon">🍽️</text>
      <text class="title-text">菜品管理</text>
    </view>
    <button class="add-btn" bindtap="addDish">
      <text class="add-icon">+</text>
      <text>添加菜品</text>
    </button>
  </view>

  <!-- 搜索栏 -->
  <view class="search-bar">
    <view class="search-input-wrapper">
      <text class="search-icon">🔍</text>
      <input
        class="search-input"
        type="text"
        placeholder="搜索菜品名称或食材..."
        value="{{searchKeyword}}"
        bindinput="onSearchInput"
        confirm-type="search"
      />
      <text wx:if="{{searchKeyword}}" class="clear-icon" bindtap="clearSearch">✕</text>
    </view>
  </view>

  <!-- 菜品列表 -->
  <view wx:if="{{loading}}" class="loading">加载中...</view>

  <view wx:elif="{{filteredDishes.length === 0 && !loading}}" class="empty-state">
    <text class="empty-icon">{{searchKeyword ? '🔍' : '📋'}}</text>
    <text class="empty-text">{{searchKeyword ? '未找到相关菜品' : '暂无菜品'}}</text>
    <text class="empty-hint">{{searchKeyword ? '试试其他关键词' : '点击上方按钮添加菜品'}}</text>
  </view>

  <view wx:else class="dish-grid">
    <view wx:for="{{filteredDishes}}" wx:key="_id" class="dish-card {{item.status === 'offline' ? 'offline' : ''}}">
      <!-- 菜品图片 -->
      <view class="dish-image-wrapper">
        <image class="dish-image" src="{{item.image || '/images/default-dish.png'}}" mode="aspectFill"></image>
        <!-- 状态标签 -->
        <view class="status-badge {{item.status}}">
          {{item.status === 'online' ? '已上架' : '已下架'}}
        </view>
      </view>

      <!-- 菜品信息 -->
      <view class="dish-info">
        <view class="dish-name">{{item.name}}</view>
        <view class="dish-category">
          <text class="category-icon">{{getCategoryIcon(item.category)}}</text>
          <text>{{getCategoryName(item.category)}}</text>
        </view>
        <!-- 显示食材（如果有） -->
        <view wx:if="{{item.ingredients && item.ingredients.length > 0}}" class="dish-ingredients">
          <text class="ingredients-label">🥬</text>
          <text class="ingredients-text">{{item.ingredients.join('、')}}</text>
        </view>
      </view>

      <!-- 操作按钮 -->
      <view class="dish-actions">
        <button class="action-btn" bindtap="editDish" data-id="{{item._id}}">
          <text class="btn-icon">✏️</text>
        </button>
        <button class="action-btn" bindtap="toggleStatus" data-id="{{item._id}}" data-status="{{item.status}}">
          <text class="btn-icon">{{item.status === 'online' ? '📦' : '📤'}}</text>
        </button>
        <button class="action-btn" bindtap="deleteDish" data-id="{{item._id}}">
          <text class="btn-icon">🗑️</text>
        </button>
      </view>
    </view>
  </view>
</view>
