# 健康数据趋势图 - 调试指南

## 问题描述
1. 数据趋势选择器（倒三角）点击后看不到选项
2. 添加了血氧记录但趋势图显示没有数据

## 调试步骤

### 1. 打开微信开发者工具控制台

在微信开发者工具中：
- 点击底部的 **"Console"** 标签
- 查看控制台输出的调试信息

### 2. 检查图表选项初始化

页面加载时应该看到类似这样的日志：

```
chartTypeOptions: Array(6)
  [0]: {type: "weight", label: "体重", unit: "kg", color: "#667eea"}
  [1]: {type: "bloodPressure", label: "血压(收缩压)", unit: "mmHg", color: "#f093fb"}
  [2]: {type: "bloodSugar", label: "血糖", unit: "mmol/L", color: "#4facfe"}
  [3]: {type: "bloodOxygen", label: "血氧", unit: "%", color: "#43e97b"}
  [4]: {type: "uricAcid", label: "尿酸", unit: "μmol/L", color: "#fa709a"}
  [5]: {type: "height", label: "身高", unit: "cm", color: "#30cfd0"}
currentChartTypeIndex: 0
```

**如果没有看到这个日志：**
- 说明 `getChartTypeOptions()` 函数没有正确执行
- 检查 `pages/health/utils/chart-helper.js` 文件是否存在

### 3. 检查健康数据加载

当你添加血氧记录后，切换到健康打卡tab，应该看到：

```
=== 更新图表数据 ===
allHealthRecords: Array(2)  // 你添加了2条血氧记录
  [0]: {type: "bloodOxygen", value: 98, recordDate: "2024-12-10 15:30:00", ...}
  [1]: {type: "bloodOxygen", value: 97, recordDate: "2024-12-10 15:32:00", ...}
allHealthRecords length: 2
currentChartTypeIndex: 0
chartTypeOptions: Array(6)
当前选择的类型: weight  // 默认显示体重
时间范围: 7 天
处理后的图表数据: Array(0)  // 因为选的是体重，但只有血氧数据
图表数据长度: 0
```

### 4. 切换到正确的指标

在页面上：
1. 点击"数据趋势"右侧的选择器（体重 ▼）
2. 如果弹出选项列表，选择 **"血氧"**
3. 如果没有弹出选项，查看控制台是否有错误

选择血氧后，应该看到：

```
=== 更新图表数据 ===
allHealthRecords: Array(2)
当前选择的类型: bloodOxygen  // 已切换到血氧
时间范围: 7 天
处理后的图表数据: Array(2)  // 有数据了！
  [0]: {date: "2024-12-10", value: 98}
  [1]: {date: "2024-12-10", value: 97}  // 同一天的会合并，取最新的
图表数据长度: 2
```

## 常见问题排查

### 问题1：选择器点不开 / 看不到选项

**可能原因：**
- `chartTypeOptions` 是空数组
- 页面渲染时出错

**解决方法：**
1. 查看控制台是否有红色错误信息
2. 检查 `chartTypeOptions` 是否正确初始化
3. 尝试重新编译项目（点击"编译"按钮）

### 问题2：图表显示"暂无数据"

**可能原因 A：选择的指标类型不对**
- 你添加了血氧记录，但图表显示的是体重

**解决方法：**
- 切换选择器到"血氧"

**可能原因 B：数据没有正确加载**

**解决方法：**
1. 查看控制台 `allHealthRecords` 是否有数据
2. 检查 `type` 字段是否正确（应该是 `bloodOxygen`）
3. 检查 `recordDate` 是否在最近7天内

### 问题3：数据格式问题

检查你添加的血氧记录格式：

正确的格式应该是：
```javascript
{
  type: "bloodOxygen",  // 注意是 bloodOxygen 不是 blood_oxygen
  value: 98,  // 数值类型
  displayValue: "98%",
  recordDate: "2024-12-10 15:30:00",  // 包含日期和时间
  ...
}
```

## 手动测试

如果自动调试失败，可以在控制台手动测试：

### 测试 1：检查数据

```javascript
// 在控制台输入
const pages = getCurrentPages()
const currentPage = pages[pages.length - 1]
console.log('allHealthRecords:', currentPage.data.allHealthRecords)
console.log('chartTypeOptions:', currentPage.data.chartTypeOptions)
console.log('currentChartData:', currentPage.data.currentChartData)
```

### 测试 2：手动切换到血氧

```javascript
// 在控制台输入
const pages = getCurrentPages()
const currentPage = pages[pages.length - 1]
currentPage.setData({ currentChartTypeIndex: 3 })  // 3 = 血氧
currentPage.updateChartData()
console.log('切换后的图表数据:', currentPage.data.currentChartData)
```

## 预期结果

正常情况下：
1. ✅ 页面加载后，选择器显示"体重 ▼"
2. ✅ 点击选择器，弹出6个选项：体重、血压、血糖、血氧、尿酸、身高
3. ✅ 选择"血氧"后，图表区域显示你的血氧数据趋势图
4. ✅ 图表上方显示统计数据：平均值、最高值、最低值、趋势

## 如果问题仍未解决

请提供以下信息：

1. **控制台截图**：包含所有日志输出
2. **页面截图**：显示数据趋势区域
3. **数据库截图**：health_records 表中的血氧记录
4. **错误信息**：控制台中的红色错误（如果有）

## 快速修复建议

如果遇到问题，尝试以下操作：

1. **清除缓存重新编译**
   - 点击"清缓存" -> "清除全部缓存"
   - 点击"编译"

2. **重启开发者工具**
   - 关闭微信开发者工具
   - 重新打开项目

3. **检查文件路径**
   - 确保 `pages/health/utils/chart-helper.js` 存在
   - 确保 `components/health-echart/` 目录完整

4. **验证血氧记录**
   - 进入"健康指标" -> "血氧" 卡片
   - 确认显示"2条记录"

---

**调试时间**：2024-12-10
**期待你的反馈！** 📊
