# 菜品图片显示问题解决方案

## 问题原因

菜品图片上传后，只有上传者能看到，其他用户看不到。这是因为**云存储的默认权限设置**导致的。

## 解决方案

### 方法一：设置云存储权限（推荐）

1. **打开微信开发者工具**
2. **点击"云开发"按钮**
3. **进入"云存储"标签页**
4. **点击右上角"权限设置"**
5. **设置权限为以下之一**：

   **开发阶段（推荐）：**
   ```
   所有用户可读，仅创建者可写
   ```

   **正式上线（推荐）：**
   ```
   所有用户可读，仅管理员可写
   ```

   或者使用自定义安全规则：
   ```json
   {
     "read": true,
     "write": "auth.openid in get('database.users.where({role:'admin'}).openid')"
   }
   ```

6. **保存设置**

### 方法二：通过云函数获取临时链接

如果不想修改云存储权限，可以通过云函数获取文件的临时下载链接。

#### 创建云函数 `getFileUrl`

创建文件：`cloudfunctions/getFileUrl/index.js`

```javascript
const cloud = require('wx-server-sdk')

cloud.init({
  env: 'cloudbase-1gdysknn57ce9b9f'
})

exports.main = async (event, context) => {
  const { fileID } = event

  try {
    const res = await cloud.getTempFileURL({
      fileList: [fileID]
    })

    return {
      success: true,
      url: res.fileList[0].tempFileURL
    }
  } catch (err) {
    console.error('获取临时链接失败', err)
    return {
      success: false,
      error: err
    }
  }
}
```

#### 修改前端代码

修改 `menu.js` 加载菜品时转换图片链接：

```javascript
async loadDishes() {
  try {
    this.setData({ loading: true })

    const res = await wx.cloud.callFunction({
      name: 'menu',
      data: {
        action: 'getList',
        status: 'online'
      }
    })

    if (res.result.success) {
      // 转换图片链接
      const dishes = await Promise.all(
        res.result.data.map(async (dish) => {
          if (dish.image && dish.image.startsWith('cloud://')) {
            const urlRes = await wx.cloud.callFunction({
              name: 'getFileUrl',
              data: { fileID: dish.image }
            })
            if (urlRes.result.success) {
              dish.image = urlRes.result.url
            }
          }
          return dish
        })
      )

      this.setData({
        dishes: dishes,
        filteredDishes: dishes,
        loading: false
      })
    }
  } catch (err) {
    console.error('加载菜品失败', err)
    this.setData({ loading: false })
    wx.showToast({
      title: '加载失败',
      icon: 'none'
    })
  }
}
```

## 推荐方案

**强烈推荐使用方法一**，直接在云开发控制台设置云存储权限为"所有用户可读，仅创建者可写"。

这是最简单、最高效的方案，不需要修改任何代码。

## 验证步骤

1. 修改云存储权限后
2. 用管理员账号上传一张菜品图片
3. 退出登录，用另一个微信账号登录
4. 查看菜单页面，应该能看到菜品图片了

## 注意事项

- 云存储文件 ID 格式：`cloud://环境ID.xxxx/文件路径`
- 确保图片已经成功上传到云存储（可以在云开发控制台的"存储"中查看）
- 如果使用临时链接方案，链接有效期为 2 小时，过期后需要重新获取

## 当前上传逻辑检查

您的代码中 `uploadImage` 函数逻辑是正确的：

```javascript
// utils/util.js - 第 117-147 行
const uploadImage = async () => {
  try {
    const chooseResult = await wx.chooseImage({
      count: 1,
      sizeType: ['compressed'],
      sourceType: ['album', 'camera']
    })

    const tempFilePath = chooseResult.tempFilePaths[0]
    const cloudPath = `dish-images/${Date.now()}-${Math.random().toString(36).substr(2, 9)}.${tempFilePath.match(/\.(\w+)$/)[1]}`

    showLoading('上传中...')

    // 上传到云存储 ✅
    const uploadResult = await wx.cloud.uploadFile({
      cloudPath,
      filePath: tempFilePath
    })

    hideLoading()
    return uploadResult.fileID  // ✅ 返回云存储文件ID
  } catch (err) {
    hideLoading()
    console.error('上传图片失败', err)
    throw err
  }
}
```

这段代码已经正确地将图片上传到云存储并返回 fileID，问题只是权限设置。

## 快速解决（1分钟）

1. 打开微信开发者工具
2. 点击顶部"云开发"
3. 点击"云存储"标签
4. 点击右上角"权限设置"
5. 选择"所有用户可读，仅创建者可写"
6. 点击保存

完成！✅
